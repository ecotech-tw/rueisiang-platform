# 執行 PRD：HRIS 申請與審核

## 1. 產品邊界

申請中心是 HRIS 的獨立工作區，不隸屬於儀表板、員工內頁或出勤紀錄。它集中處理跨出勤與薪資的申請工作，但不把不同業務的資料合併成一張萬用表。假別主檔則放在「出勤／假別管理」，不塞進申請中心的新增表單。

管理端路徑為 `/hr/requests`，目前提供：

- HR 代員工建立請假申請
- HR 代員工建立加班申請
- 查看請假、加班與補打卡申請紀錄
- 查看待審核申請並執行核准、駁回或取消

員工頁與出勤頁可以提供帶入員工脈絡的捷徑，但申請紀錄與生效狀態只有申請中心是主要入口。

## 2. 狀態契約

```text
draft → pending → approved / rejected
pending → cancelled
```

目前 HRIS 管理端代登採直接核准：建立資料時保存 `approved`、建立者、核准者與核准原因。狀態欄位與審核 API 保留 `pending` 階段，供未來 HR 前台由員工提出申請後交給主管或 HR 審核。

「建立申請」不等於任意覆寫出勤資料。只有核准的申請才會成為出勤／薪資計算可讀取的正式來源。

## 3. 業務資料邊界

請假與加班各自保存具體欄位、任職 FK、申請期間、給薪或結算方式與審核資料；補打卡沿用既有補打卡申請表。申請中心的統一列表由服務層組合，不使用 `entity_type + entity_id` 或任意 `payload_json` 取代業務欄位。

核准後的效果依申請類型處理：

- 請假：保存核准假勤區間，出勤與薪資查詢讀取核准資料。
- 加班：以員工與加班時段為主，不要求填寫營運據點；保存核准時段，付薪加班進入薪資試算，補休保留結算方式。
- 補打卡：核准後新增人工打卡事件，不覆寫原始事件。

## 4. 權限與未來入口

管理端申請中心目前使用 `hr:request:review`，因為建立申請與審核都屬 HR 管理操作。本人入口不接受前端指定其他員工，建立者與申請對象分開保存或由既有稽核事件追溯。

未來 HR 前台只需把建立端切換為 `pending`，不需要另建一套申請資料模型；主管與 HR 管理端仍透過同一組審核服務處理。

## 5. 與月度人工登記的關係

「薪資／月度資料登記」是薪資結算前的人工調整入口，不是請假申請中心。核准的正式請假申請與月度人工登記不可在同一筆業務上重複扣款；兩者的優先順序與結帳後調整規則由薪資計算服務統一處理。
