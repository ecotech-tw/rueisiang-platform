{
  "name": "@rueisiang/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "dev": "tsx watch src/dev/server.ts",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@rueisiang/auth": "workspace:*",
    "@rueisiang/cyberbiz": "workspace:*",
    "@rueisiang/db": "workspace:*",
    "hono": "^4.9.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@rueisiang/config": "workspace:*",
    "@types/node": "^22.19.19",
    "drizzle-orm": "0.45.2",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "4.92.0"
  }
}
